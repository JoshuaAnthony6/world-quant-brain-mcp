# BRAIN MCP Server - 独立运行版本

## 快速开始

### Windows
双击运行 `启动MCP服务.bat`

### Linux/Mac
```bash
chmod +x 启动MCP服务.sh
./启动MCP服务.sh
```

## 手动运行

```bash
# 1. 安装依赖
pip install -r requirements.txt
playwright install chromium
playwright install-deps chromium

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 填入您的BRAIN凭证

# 3. 启动服务
python main.py
```

## 配置说明

### 必需配置 (.env)

```
CREDENTIALS_EMAIL=your_email@example.com
CREDENTIALS_PASSWORD=your_password
```

### 可选配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| MCP_HOST | 127.0.0.1 | 服务监听地址 |
| MCP_PORT | 8000 | 服务监听端口 |
| REDIS_HOST | localhost | Redis主机 |
| REDIS_PORT | 6379 | Redis端口 |
| MCP_CONFIG_FILE | user_config.json | 配置文件路径 |

## 服务地址

- MCP Server: http://localhost:8000/mcp
- 健康检查: http://localhost:8000/health

## 依赖说明

- **必需**: playwright, requests, pydantic, mcp, beautifulsoup4, pandas, python-dotenv, email-validator
- **可选**: redis (用于缓存，可提升性能)

## Docker版本 vs 独立版本

| 特性 | Docker版本 | 独立版本 |
|------|------------|----------|
| 依赖 | 自动安装 | 手动安装 |
| Redis | 自动配置 | 可选 |
| Playwright | 自动配置 | 需手动安装浏览器 |
| 配置文件 | /app/config/ | ./user_config.json |

## 故障排除

### Playwright安装失败
```bash
# Ubuntu/Debian
playwright install-deps chromium

# Windows
playwright install chromium
```

### Redis连接失败
- 不影响核心功能，只是缓存不可用
- 如需启用，安装Redis并确保服务运行

### 端口被占用
修改 .env 中的 MCP_PORT 或环境变量：
```bash
set MCP_PORT=8001
python main.py
```
